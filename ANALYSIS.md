# Анализ повторяющихся частей в страницах printing

## Резюме

После анализа всех страниц в `pages/printing/` было выявлено множество повторяющихся паттернов, которые можно вынести в переиспользуемые компоненты и composables.

## Найденные повторяющиеся части

### 1. Логика формы заказа (Script Setup)

**Повторяется на всех страницах:**

- Импорты:
  ```typescript
  import { reactive, computed, ref } from "vue";
  import type { OrderField } from "~/types/order-fields";
  import { calculateTotalPrice, getQuantityFromFields } from "~/types/order-fields";
  ```

- `definePageMeta` - идентичный на всех страницах
- `useHead` - структура одинаковая, только title и description отличаются
- `fields` - структура одинаковая (reactive<OrderField[]>), но конфигурация разная (это специфично для каждой страницы)
- `isDesignActive` - ref(false), одинаковый
- `designPrice` - значение разное, но логика одинаковая
- `totalPrice` - computed с одинаковой логикой
- `macketFile`, `macketFileName`, `handleFileUpload`, `removeFile` - идентичные на всех страницах
- `formData` - reactive объект с одинаковыми полями (name, phone, email)
- `showToast`, `toastMessage` - идентичные
- `submitOrder` - функция очень похожая, отличается только `productType` и `printType`

**Решение:** Создан composable `useOrderForm.ts` ✅

### 2. Навигация по табам (Template)

**Структура повторяется:**

```vue
<div class="tab-btn-con">
  <NuxtLink
    to="/path"
    class="tab-btn"
    :class="{ active: $route.path === '/path' }"
  >
    Label
  </NuxtLink>
  <!-- ... еще табы ... -->
</div>
```

**Различия:**
- Количество табов разное (2-3 таба)
- Пути и лейблы разные

**Решение:** Создан компонент `TabNavigation.vue` ✅

### 3. Кнопки опций (Template)

**Повторяется идентично на всех страницах:**

```vue
<div class="tab-option-btn-con">
  <button class="tab-option-btn">
    Технические требования к макету
  </button>
  <button class="tab-option-btn">Примеры работ</button>
  <button class="tab-option-btn">
    Срок изготовления: <span>один рабочий день</span>
  </button>
</div>
```

**Решение:** Создан компонент `TabOptionButtons.vue` ✅

### 4. Общие стили (Style Scoped)

**Повторяющиеся стили:**

- `.tab-main` - идентичные стили на всех страницах
- `.tab-btn`, `.tab-btn-con`, `.tab-btn.active` - идентичные
- `.tab-option` - идентичные
- `.tab-option-img` - идентичные (некоторые страницы имеют `padding: 2%`, но это незначительно)
- `.tab-option-btn-con`, `.tab-option-btn` и все связанные стили - идентичные

**Специфичные стили (не повторяющиеся):**
- `.book-btn-*` - только для страниц буклетов (laser-print, ofset-print)
- SVG анимации для book-btn - только для буклетов

**Решение:** Создан файл `printing-page-common.css` ✅

### 5. Общая структура Template

**Повторяющаяся структура:**

```vue
<div class="main-content-con">
  <div class="main-content">
    <div class="tab-con">
      <!-- TabNavigation -->
      <div class="tab-main">
        <div class="tab-option">
          <div class="tab-option-img">
            <!-- Изображение или book-btn-con -->
          </div>
          <TabOptionMain :fields="fields" />
          <!-- TabOptionButtons -->
        </div>
        <TabOrder ... />
      </div>
    </div>
  </div>
</div>
<Toast ... />
```

**Различия:**
- Изображение в `tab-option-img` разное
- Некоторые страницы имеют `book-btn-con` вместо простого изображения
- Props для `TabOrder` разные (title, subTitle, showDesignButton)

## Рекомендации по рефакторингу

### ✅ Уже создано:

1. **`composables/useOrderForm.ts`** - логика формы заказа
2. **`components/TabNavigation.vue`** - навигация по табам
3. **`components/TabOptionButtons.vue`** - кнопки опций
4. **`assets/css/printing-page-common.css`** - общие стили

### 🔄 Следующие шаги:

1. **Рефакторинг страниц:**
   - Заменить повторяющуюся логику на `useOrderForm`
   - Заменить навигацию на `<TabNavigation>`
   - Заменить кнопки на `<TabOptionButtons>`
   - Импортировать общие стили через `<style>`

2. **Опционально - создание базового компонента:**
   - Можно создать `PrintingPageLayout.vue` для общей структуры
   - Но это может быть избыточно, так как структура не на 100% идентична

### 📝 Особенности страниц:

- **booklet/laser-print.vue и booklet/ofset-print.vue:**
  - Имеют специфичную логику для `book-btn` (SVG анимация, синхронизация с dropdown)
  - Имеют специфичную логику auto-activate для "Биговка"
  - Стили для `.book-btn-*` должны остаться в этих страницах

- **lamination/doc.vue и lamination/large.vue:**
  - Используют `PageDetail` компонент
  - Имеют `showDesignButton={false}`

- **replication/index.vue:**
  - Имеет `subTitle` для `TabOrder`
  - Отсутствует `submitOrder` (но структура готова)

- **more.vue страницы:**
  - Имеют только навигацию и `PageDetail`
  - Не имеют формы заказа

## Статистика дублирования

- **Строк кода в script setup:** ~150-200 строк повторяются на каждой странице
- **Строк кода в template:** ~50-80 строк повторяются
- **Строк кода в style:** ~150-200 строк повторяются
- **Всего страниц:** 12 страниц с формой заказа
- **Потенциальная экономия:** ~4000-5000 строк кода

